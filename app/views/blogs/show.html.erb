<h2 class='blog_new_edit_show_h' >ブログ詳細</h2>
<p>投稿者: <%= @blog.user.name %></p>
<p>タイトル: <%= @blog.title %></p>
<p>内容:</p>
<p><%= @blog.content %></p>
<p><%= image_tag @blog.image.url, size: '200x200' %></p>
<% unless @blog.user_id == current_user.id %>
  <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する', favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする', favorites_path(blog_id: @blog.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %>
<%= link_to '一覧に戻る', blogs_path, class: 'btn btn-default' %>